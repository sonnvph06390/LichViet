<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.thuanthanh.lichviet">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.BoitinhduyenActivity"></activity>
        <activity android:name=".fragment.SuKienFragment" />
        <activity android:name=".activity.TaoSuKienActivity" />
        <activity android:name=".activity.VankhanActivity" />
        <activity android:name=".activity.TuvihangngayActivity" />
        <activity
            android:name=".activity.VietSuKienActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".fragment.SuKiennFragment"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activity.Tuvi2019Activity" />
        <activity
            android:name=".activity.MonthViewActivity"
            android:theme="@style/AppThemeNoBar" />
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>